//c file
#include <stdio.h>
#include <stdlib.h>
#include "LabExam.h"
//
//reallly need the .h file to see what i have to do here i am blind basically cuz idk what functions to use so ima anotate
//but it looks like your dealling with a linked list changed up with different names so just remember to think about it logically
population_age_t * setup_pop(unsigned int anArraySize){
	population_age_t * pop = malloc(sizeof(population_age_t));
//no need to check is pop is NULL ur seting it up 
	if(pop!=NULL){
		pop->ages = malloc(sizeof(int)*anArraySize);
		//same thing here 
		if(pop->ages!=NULL){
			pop->arraySize = anArraySize;
			pop->numOfAges = 0;
			
		}
		//dont free pop because ur setting it up 
	}

	return pop;
}

void destroy_pop(population_age_t * pop){ //free all memory
	population_age_t * temp = pop->arraySize; // temporary node of pop
	
	while(temp){
		free(pop);
	}


	//
}

int insert_age(population_age_t * pop, int anAge){
	//appending 
	//is this really the name of the new node? because it looks lik the name of a population age list idk cuz i dont have .h
	population_age_t * temp = malloc(sizeof(population_age_t)); //new node
	temp->age = anAge;
	if(pop->anArraySize == NULL){
		pop->anArraySize = temp;
	}
	else{
		return 0;
	}

}

int same_age(population_age_t * pop, int targetAge){ //check if same age in numof ages
	population_age_t * temp = pop->numOfAges

	int len = pop->numOfAges;


}

void print_pop(population_age_t * pop){ 
//psydo
	while(){
	printf("%d",temp);
	temp=temp->next;
	
	}

}

// int main(void){
// 	population_age_t * pop1 = setup_pop(7);
// 	//
// 	return 0;
// }
where you given anything else?
what is the node called cuz u are mixing up the list with the node

need .h file
